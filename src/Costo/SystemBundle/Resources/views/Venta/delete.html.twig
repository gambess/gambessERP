<center>¿Esta Seguro que desea borrar la venta del día {{ venta.FechaVenta|date('d/m/Y') }}?</center>
<form id="delete" action="{{ path('delete_venta', { 'id': venta.IdDetalle }) }}" method="post">
            {{ form_widget(delete_form) }}
</form>
