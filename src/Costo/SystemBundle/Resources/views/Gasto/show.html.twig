{% extends 'CostoSystemBundle::layout.html.twig' %}

{% block content %}
{% block title %}Datos del Gasto {{ gasto.NombreGasto }}{% endblock %}

<h1>{{ block('title') }}</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Codigo</th>
            <td>{{ gasto.IdGasto }}</td>
        </tr>
        <tr>
            <th>Nombre del Gasto</th>
            <td>{{ gasto.NombreGasto }}</td>
        </tr>
        <tr>
            <th>Costo</th>
            <td>{{ gasto.CostoGasto }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('backend_ciudad') }}">
            Volver al listado
        </a>
    </li>
    <li>
        <a href="{{ path('backend_ciudad_edit', { 'id': gasto.IdGasto }) }}">
            Modificar ciudad
        </a>
    </li>
    <li>
        <form action="{{ path('backend_ciudad_delete', { 'id': gasto.IdGasto }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Borrar ciudad</button>
        </form>
    </li>
</ul>
{% endblock %}